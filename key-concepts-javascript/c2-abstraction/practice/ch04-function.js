/* =====04. 함수 부려먹기I===== */
function logParticipant(userName) {
  console.log(`${userName}(이)가 대화에 참여했습니다.`);
}

logParticipant('동수');
logParticipant('윤하');
logParticipant('재준');
logParticipant('동훈');
logParticipant('영희');
logParticipant('신욱');
